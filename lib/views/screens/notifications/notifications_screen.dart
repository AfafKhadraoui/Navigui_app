import 'package:flutter/material.dart';

/// Notifications Screen

/// List of all notifications, mark as read, clear all
/// Notification types: new job match, application response, job expiring soon, new applicant
class NotificationsScreen extends StatelessWidget {
  const NotificationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement notifications list
    // TODO: Add mark as read
    // TODO: Add clear all option
    return const Scaffold(body: Center(child: Text('Notifications Screen')));
  }
}
