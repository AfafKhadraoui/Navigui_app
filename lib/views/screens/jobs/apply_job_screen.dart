import 'package:flutter/material.dart';

/// Apply Job Screen

/// - Upload CV if required
/// - Write cover message
/// - Confirm availability
class ApplyJobScreen extends StatelessWidget {
  const ApplyJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement application form
    // TODO: Add CV upload
    // TODO: Add cover message field
    // TODO: Add submit button
    return const Scaffold(body: Center(child: Text('Apply Job Screen')));
  }
}
