import 'package:flutter/material.dart';

/// Saved Jobs Screen

class SavedJobsScreen extends StatelessWidget {
  const SavedJobsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement saved jobs list
    // TODO: Add remove bookmark option
    return const Scaffold(body: Center(child: Text('Saved Jobs Screen')));
  }
}
