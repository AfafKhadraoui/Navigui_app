import 'package:flutter/material.dart';

/// Onboarding Screen
/// Shows app features to new users
class OnboardingScreen extends StatelessWidget {
  const OnboardingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement onboarding flow with PageView
    // TODO: Add skip and next buttons
    // TODO: Add navigation to welcome screen
    return const Scaffold(body: Center(child: Text('Onboarding Screen')));
  }
}
