import 'package:flutter/material.dart';

/// Choose Account Type Screen

class ChooseAccountTypeScreen extends StatelessWidget {
  const ChooseAccountTypeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement account type selection
    // TODO: Add Student and Employer options
    // TODO: Add navigation based on selection
    return const Scaffold(
      body: Center(child: Text('Choose Account Type Screen')),
    );
  }
}
