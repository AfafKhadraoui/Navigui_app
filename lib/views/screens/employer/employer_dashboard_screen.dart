import 'package:flutter/material.dart';

/// Employer Dashboard Screen

/// + button to add new, view all posts
class EmployerDashboardScreen extends StatelessWidget {
  const EmployerDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement employer dashboard
    // TODO: Add posted jobs list
    // TODO: Add floating action button for new job
    return const Scaffold(
      body: Center(child: Text('Employer Dashboard Screen')),
    );
  }
}
