import 'package:flutter/material.dart';

/// Create Job Screen

/// Category, title, description, requirements, pay, location, deadline
/// Mark as urgent, require CV, save as draft
class CreateJobScreen extends StatelessWidget {
  const CreateJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement job creation form
    // TODO: Add all required fields
    // TODO: Add save as draft option
    return const Scaffold(body: Center(child: Text('Create Job Screen')));
  }
}
