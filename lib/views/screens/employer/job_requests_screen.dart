import 'package:flutter/material.dart';

/// Job Requests Screen

/// List of students who applied, sort by date, filter options
class JobRequestsScreen extends StatelessWidget {
  const JobRequestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement applications list
    // TODO: Add sort and filter options
    return const Scaffold(body: Center(child: Text('Job Requests Screen')));
  }
}
